package com.codedsolutions47.remitonewrapper.controller;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/api/remitters")
public class RemitterController {

//    @PostMapping
//    public ResponseEntity<Remitter> createRemitter(@RequestBody RemitterRequest request) {
//        // Call the service method to create a new remitter
//        Remitter remitter = remitterService.createRemitter(request);
//        return ResponseEntity.ok(remitter);
//    }
//
//    @GetMapping("/{id}")
//    public ResponseEntity<Remitter> searchRemitter(@PathVariable Long id) {
//        // Call the service method to search for an existing remitter
//        Remitter remitter = remitterService.searchRemitter(id);
//        return ResponseEntity.ok(remitter);
//    }


}
